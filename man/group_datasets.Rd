% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pooling.R
\name{group_datasets}
\alias{group_datasets}
\title{Group dose-response datasets into mutually poolable sets}
\usage{
group_datasets(
  datasets,
  models = c("exponential", "beta_poisson"),
  alpha = 0.05,
  method = c("agglomerative", "exhaustive"),
  max_exhaustive = 6L
)
}
\arguments{
\item{datasets}{A named list of dose-response data frames (accepted by
\code{\link[=as_dose_response]{as_dose_response()}}) or already-standardized tibbles. Unnamed elements are
named \code{dataset_1}, \code{dataset_2}, and so on.}

\item{models}{Character vector of models, a subset of \code{"exponential"},
\code{"beta_poisson"}, and \code{"exact_beta_poisson"}.}

\item{alpha}{Significance level for the chi-squared test.}

\item{method}{Grouping strategy. \code{"agglomerative"} (default) starts from
singletons and repeatedly merges the most compatible poolable pair until no
pair is poolable; it scales to many datasets. \code{"exhaustive"} enumerates all
set partitions and picks the coarsest one whose every group is poolable; it
is limited to \code{max_exhaustive} datasets.}

\item{max_exhaustive}{Maximum number of datasets allowed for the exhaustive
method (the number of set partitions grows super-exponentially).}
}
\value{
A long tibble with columns \code{dataset}, \code{model}, and integer \code{group}.
The per-model matrix of pairwise pooling p-values is attached as the
\code{"pairwise"} attribute.
}
\description{
Assigns datasets to groups such that the datasets within a group are
statistically poolable (via the \code{\link[=poolability_test]{poolability_test()}} likelihood-ratio test)
and datasets in different groups are not. Grouping is performed per model,
because poolability can differ between the exponential and beta-Poisson
models.
}
