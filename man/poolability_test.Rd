% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pooling.R
\name{poolability_test}
\alias{poolability_test}
\title{Test whether dose-response datasets are statistically poolable}
\usage{
poolability_test(
  datasets,
  models = c("exponential", "beta_poisson"),
  alpha = 0.05
)
}
\arguments{
\item{datasets}{A named list of dose-response data frames (accepted by
\code{\link[=as_dose_response]{as_dose_response()}}) or already-standardized tibbles. Unnamed elements are
named \code{dataset_1}, \code{dataset_2}, and so on.}

\item{models}{Character vector of models, a subset of \code{"exponential"},
\code{"beta_poisson"}, and \code{"exact_beta_poisson"}.}

\item{alpha}{Significance level for the chi-squared test.}
}
\value{
A tibble with one row per model: \code{model}, \code{n_datasets},
\code{deviance_pooled}, \code{deviance_unpooled}, \code{lrt_statistic}, \code{df},
\code{chi_square_critical}, \code{p_value}, \code{poolable}, \code{converged}, and a
human-readable \code{conclusion}.
}
\description{
Runs the poolability likelihood-ratio test (Haas, Rose & Gerba): for each
model, fit every dataset separately (unpooled) and the stacked combination
(pooled), then compare the deviance difference to a chi-squared distribution.
Datasets are poolable when the pooled model does not fit significantly worse
than the separate models, i.e. one shared parameter set is adequate.
}
\details{
Datasets are combined by stacking their rows (each dataset's dose groups are
kept as separate binomial observations); they are not aggregated, so repeated
doses across datasets are preserved.
}
